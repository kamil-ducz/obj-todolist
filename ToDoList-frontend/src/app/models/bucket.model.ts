export type Bucket = {
    id: number;
    name: string;
    description: string;
    bucketColorId: number;
    bucketColor: string;
    bucketCategoryId: number;
    bucketCategory: string;
    maxAmountOfTasks: number;
    isActive: boolean;
}